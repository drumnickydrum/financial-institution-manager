import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  skeletonContainer: {
    width: '100%',
    marginBottom: '2rem',
  },
  skeletonText: {
    height: '25px',
  },
});

export default useStyles;
